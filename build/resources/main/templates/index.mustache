{{>layout/header}}
<h1>스프링 부트로 시작하는 웹 서비스</h1>

<div class="col-md-12">
    <div class="row">
        <div class="col-md-6">
            <a href="/posts/save" role="button" class="btn btn-primary">글 등록</a>

            <!--머스테치에 if문 없음. true,false만 판단 > 그래서 항상 최종값을 넘겨줘야함.-->
            {{#userName}}<!--# :: 값이 있는 경우 true 리턴-->
                Logged in as: <span id="user">{{userName}}</span>
                <a href="/logout" class="btn btn-info active" role="button">Logout</a><!--/logout :: 스프링 시큐리티 기본제공 로그아웃 URL!!-->
            {{/userName}}
            {{^userName}}<!--^ :: 값이 없는 경우 true 리턴-->
                <a href="/oauth2/authorization/google" class="btn btn-success active" role="button">Google Login</a><!--/oauth2/authorization/ :: 스프링 시큐리티 기본제공 로그인 URL!!-->

                <a href="/oauth2/authorization/naver" class="btn btn-secondary active" role="button">Naver Login</a><!--경로 : application-oauth.properties파일에서 redirect-uri에 지정한 값에 맞춰 자동으로 등록됨.-->
            {{/userName}}
        </div>
    </div>
    <br>
    <!--목록 출력 영역-->
    <table class="table table-horizontal table-bordered">
        <thead class="thead-strong">
        <tr>
            <th>게시글번호</th>
            <th>제목</th>
            <th>작성자</th>
            <th>최종수정일</th>
        </tr>
        </thead>
        <tbody>
        {{#posts}}      <!--// 머스테치 문법 : posts라는 List를 순회한다.-->
            <tr>
                <td>{{id}}</td>
                <td><a href="/posts/update/{{id}}">{{title}}</a></td>
                <td>{{author}}</td>
                <td>{{modifiedDate}}</td>
            </tr>
        {{/posts}}
        </tbody>
    </table>
    <!--//목록 출력 영역-->
</div>
{{>layout/footer}}